{% extends 'base.html.twig' %}

{% block body %}
<div class="container-md col-md-8">
    <a class="btn btn-dark" href="{{ path('book_add')}}">
        Add book
    </a>
    <table class="table table-striped|sm|bordered|hover|inverse table-inverse table-responsive">
        <thead class="thead-inverse|thead-default">
            <tr>
                <th>Book ID</th>
                <th>Book Title</th>
                <th>Book Cover</th>
                <th>Actions</th>
            </tr>
            </thead>
            <tbody>
            {% for book in books %}
                <tr>
                    <td scope="row">{{ book.id }}</td>
                    <td>{{ book.title }}</td>
                    <td>
                        <a href="{{ path('book_detail', {'id': book.id})}}">
                            <img src="{{ asset('covers/'~book.cover) }}" width="100" height="120">
                        </a>
                    </td>
                    <td>
                        <a class="btn btn-outline-primary" href="{{ path('book_edit', {'id': book.id})}}">
                            Edit book
                        </a>
                        <a class="btn btn-outline-danger" href="{{ path('book_delete', {'id': book.id})}}">
                            Delete book
                        </a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
    </table>
    </div>
    
{% endblock %}
